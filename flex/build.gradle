buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath group: 'org.gradlefx', name: 'gradlefx', version: '0.4'
    }
}

subprojects {
    apply plugin: 'gradlefx'


    srcDirs = [
	    'src/main/flex' //defaults to src/main/actionscript
    ]
}